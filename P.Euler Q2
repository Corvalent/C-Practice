#include <iostream>
using namespace std;

int main()
{
  int answer=0, i=0, j=1, k;
    while (k<4000000)
        {
            k = i + j;
            i = j;
            j = k;
            if (k%2==0)
                answer = answer + k;
        }
    
    cout << answer;
}
